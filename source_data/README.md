# Compact figure source data

This directory contains compact source files specific to Fig. 7:

- `Figure7_variant_metrics.tsv` — mismatch counts and 49-bp sequence identity used for Fig. 7C.
- `Figure7_logo_model_metadata.csv` — exact representative JASPAR models used for Fig. 7D.

The exact source export for every final supplementary-table worksheet is under `../supplementary_table_source/`. Large reference and sequencing inputs are documented in `../INPUT_PROVENANCE.tsv`.
